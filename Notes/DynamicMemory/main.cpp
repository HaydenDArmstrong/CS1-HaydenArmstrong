#include <iostream>

int main(void)
{
    int n, m;

    std::cout << "Enter size of table seperated by spaces (rows columns)"
    << std::endl;

    std::cin >> rows >> columns;

    double *distances = new double[rows][columns];

    for (int i=0;i<rows; i++)
    {
        for (int j=0; j<columns; j++)
        {
            double *address_current_val
        }
    }
}